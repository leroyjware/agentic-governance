# API — Phase 1

FastAPI application with governance middleware chain.

```
POST /chat          — governed agent interaction
GET  /health        — health check
GET  /metrics       — Prometheus scrape endpoint
```
