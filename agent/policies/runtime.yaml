# Optional local overrides (documentation only).
# Canonical tool allowlist: harness/harness.jsonld → sh:Policy healthcare-tool-policy
# Enforced at runtime via agent.harness_loader.LoadedHarness.allowed_tools_for_agent
#
# Identity:
#   X-User-Scope header preferred; AUTH_STRICT=1 requires the header.
# Audit:
#   AUDIT_LOG_PATH=data/audit.jsonl (default) or "off" for tests/CI
